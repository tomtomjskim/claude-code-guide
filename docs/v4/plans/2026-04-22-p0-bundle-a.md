# P0 Bundle A Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `docs/v4/strategy.md` §3 P0 백로그 중 4건(P0-1/P0-2/P0-4/P0-5)을 단일 PR·단일 커밋으로 구현하여 v4.0 coherence 부채의 첫 슬라이스를 청산한다.

**Architecture:** 네 항목 모두 `scripts/validate-system.sh`를 공통 수정점으로 교차하므로 분할 시 중간 상태에서 validate가 실패한다. 따라서 (a) agents/*.md 헤더 일괄 rename → (b) `agents/pm.md` 생성 → (c) `prompts/qa.md` rename + 참조 업데이트 → (d) `validate-system.sh` 동적 파싱 리팩터링 순으로 내부 단계를 배열하고, 단일 커밋으로 묶는다. Breaking changes는 strategy.md §2 Migration Note(B1, B3)를 커밋 메시지에 포함.

**Tech Stack:** Bash(grep/awk/sed/git mv), Markdown frontmatter, YAML(읽기 전용 파싱). **테스트 프레임워크 없음** — 검증은 `bash scripts/validate-system.sh` 실행이 0 errors/0 warnings로 종료되는 것과 `bash scripts/install-skills.sh --team` 후 동일 검증이 통과함.

**Spec:**
- [`docs/v4/strategy.md`](../strategy.md) §3 P0-1/P0-2/P0-4/P0-5 (감사 스냅샷 — 실제 상태와 불일치 시 즉시 검증 후 plan 반영)
- [`.audit/result-W1.md`](../../../.audit/result-W1.md) C1, H2, H5 (원시 증거)
- [`.audit/result-W3.md`](../../../.audit/result-W3.md) V2 (SSOT 위반 상세)

**Preserved constraints:**
- 한국어 기조 유지
- `.claude/hooks/` 제거 후 상태(커밋 31b0a17) 유지 — 이번 범위 아님
- `hooks/scripts/*.sh` rename(P1-5, B2)은 이번 범위 아님
- 버전 문자열 centralization(P1-1)은 이번 범위 아님

**Verified pre-conditions (2026-04-22 기준):**
- `agents/` 디렉터리 = 15개 파일, `pm.md` 부재 (strategy P0-1 주장 확인)
- `prompts/` 디렉터리 = 16개 파일, `pm.md` + `qa.md`(단축명) 존재 (strategy P0-5 주장 확인)
- `agents/*.md` 15개 전부 `## v3.0 Template` 헤더 보유 (strategy P0-2 주장 확인)
- `scripts/validate-system.sh:43` → `## v3.0 Template` 기대 (strategy P0-2 주장 확인)
- `prompts/qa.md` 참조 전수: `agents/qa-engineer.md:33`, `docs/v3-changelog.md:37`, `docs/v3-changelog.md:128` 3곳 (strategy P0-5가 언급한 "docs/" 참조 = 2곳, 총 3곳으로 확정)
- `agents.yaml`에서 agent 이름 추출 파서 검증 완료:
  ```bash
  awk '/^agents:$/{in_block=1; next} in_block && /^[a-z_]+:/{in_block=0} in_block && /^  [a-z-]+:$/{gsub(/^  |:$/,""); print}' agents.yaml
  ```
  → 정확히 16개 추출: `pm`, `architect`, `developer`, `qa-engineer`, `dba`, `designer`, `publisher`, `documenter`, `explorer`, `security-reviewer`, `performance-reviewer`, `test-coverage-reviewer`, `accessibility-reviewer`, `ux-reviewer`, `api-reviewer`, `code-reviewer`. yq 의존성 없음 (Decision 3 준수).
  - **Gate 정확성**: `in_block=1`은 `^agents:$`(line 263)에서 on, `in_block=0`은 `^[a-z_]+:`(다음 top-level, line 635 `quality_gates:`)에서 off. 중첩 키(`    responsibilities:`, `    handoff:` 등)는 4-space 인덴트이므로 `^  [a-z-]+:$`(2-space) 패턴에 매칭 안됨. `agents.yaml` 전체에는 2-space bare 키가 25개 존재하지만 그중 16개만 `agents:` 블록 내부이며 나머지 9개(`  defaults:`, `  operations:`, `  pairs:`, `  execution:`, `  realtime:`, `  checkpoints:`, `  final:`, `  review:`, `  levels:`)는 다른 top-level 블록 소속이라 gate에 의해 제외됨. 검증 증거: 2026-04-22 실측.
- validate-system.sh 하드코딩 분류:
  - **Line 74, 82, 90**: `developer qa-engineer` / `security-reviewer ... code-reviewer` / `explorer` — 특정 속성(isolation·memory·disallowedTools) 선택 검증, 설계 의도이지 drift 재생산 엔진 아님. **동적 파싱 대상 아님**.
  - **Line 217**: `REVIEWER_PROMPTS=(... "qa")` — 리뷰어 8개 + qa 단축명. drift surface 핵심. **동적 파싱 대상**.
  - **Line 237**: `pm.md dba.md publisher.md documenter.md explorer.md designer.md` skip 리스트 — domain-specific(비-리뷰어·비-엔지니어) 에이전트 집합. 에이전트 추가 시 drift 재생산. **동적 파싱 대상**.
  - **Line 203**: `PM_PROMPT="$PROMPTS_DIR/pm.md"` — 단일 pm은 본질적으로 동적화 불필요. **유지**.

---

## File Structure

### Created

- `agents/pm.md` — PM 에이전트 Claude Code subagent 정의 파일 (frontmatter + 핵심 역할 + `## Template` + `## Boundary`)
- `docs/v4/plans/2026-04-22-p0-bundle-a.md` — 이 문서

### Modified

- `scripts/validate-system.sh` (P0-2 / P0-4)
  - Line 38-53 블록: `## v3.0 Template` → `## Template` 기대 (P0-2)
  - 스크립트 상단 초기화 블록: `ALL_AGENTS`/`REVIEWERS`/`DOMAIN_SPECIFIC` 동적 변수 추가 (P0-4)
  - Line 217 REVIEWER_PROMPTS: 동적 `$REVIEWERS` + qa-engineer (P0-4)
  - Line 237 skip 리스트: 동적 `$DOMAIN_SPECIFIC` (P0-4)
  - 신규 체크 추가: "agents.yaml ↔ agents/*.md 파일 매칭" (P0-1이 보장)
- `agents/accessibility-reviewer.md` + 14개 동일: `## v3.0 Template` → `## Template` (P0-2, 총 15 파일)
- `agents/qa-engineer.md:33`: `~/.claude/team/prompts/qa.md` → `~/.claude/team/prompts/qa-engineer.md` (P0-5)
- `docs/v3-changelog.md:37`: `` `qa.md` — QA Engineer `` → `` `qa-engineer.md` — QA Engineer `` (P0-5)
- `docs/v3-changelog.md:128`: `` QA Engineer (`qa.md`) — 330 lines `` → `` QA Engineer (`qa-engineer.md`) — 330 lines `` (P0-5)

### Renamed (git mv)

- `prompts/qa.md` → `prompts/qa-engineer.md` (P0-5, B1)

### Not modified (out of scope — 명시적으로 확인하고 제외)

- `README.md` v3.3 표기 / `CLAUDE.md` v3.2 표기 / `validate-system.sh:2,20,99,412,416` 버전 문자열 — P1-1 (version canonical 통일)
- `hooks/scripts/*.sh` 파일명 — P1-5, B2
- README.md 에이전트 목록 테이블 — P1-3
- skills/*/SKILL.md 프리셋 `(v3.0)` 레이블 — P1-2, P1-8
- `.audit/` 원시 감사 자료 — 휘발성, 내용 변경 금지

---

## Task 1: `agents/*.md` 헤더 `## v3.0 Template` → `## Template` 일괄 rename (P0-2)

**Why first:** 이후 Task 2(agents/pm.md 생성)에서 새로 만드는 파일이 어느 컨벤션을 따를지 결정하기 위해, 먼저 기존 15개를 새 컨벤션으로 전환한다. validate-system.sh의 기대값은 Task 3에서 동반 수정하므로 이 단계 직후에는 validate가 ERROR를 찍는 것이 **정상**(의도된 중간 상태).

**Files:**
- Modify: `agents/accessibility-reviewer.md`, `agents/api-reviewer.md`, `agents/architect.md`, `agents/code-reviewer.md`, `agents/dba.md`, `agents/designer.md`, `agents/developer.md`, `agents/documenter.md`, `agents/explorer.md`, `agents/performance-reviewer.md`, `agents/publisher.md`, `agents/qa-engineer.md`, `agents/security-reviewer.md`, `agents/test-coverage-reviewer.md`, `agents/ux-reviewer.md` (15 파일)

- [ ] **Step 1: 헤더 rename 일괄 실행**

```bash
cd /Users/jeongsik/develop/claude-code-guide
for f in agents/*.md; do
  # 매크로 단위로 정확 매칭 (line 시작 + '## v3.0 Template' + 끝)
  sed -i.bak 's/^## v3.0 Template$/## Template/' "$f"
done
rm agents/*.md.bak
```

- [ ] **Step 2: 검증 — 모든 15개 파일이 정확히 1회 rename되었는지**

```bash
# 모든 파일이 `## Template` 보유, `## v3.0 Template`는 0건
grep -c "^## Template$" agents/*.md
# 기대: 각 파일당 1
grep "^## v3.0 Template$" agents/*.md
# 기대: 출력 없음
```

- [ ] **Step 3: 부수 확인 — 같은 헤더 문자열이 다른 파일에도 남아있는지**

```bash
grep -rn "## v3.0 Template" /Users/jeongsik/develop/claude-code-guide/ \
  --include="*.md" --include="*.sh" 2>/dev/null \
  | grep -v ".audit/\|docs/v4/"
# 기대: scripts/validate-system.sh:43 한 줄만 (Task 3에서 수정 예정)
```

---

## Task 2: `agents/pm.md` 생성 (P0-1)

**Why:** `agents.yaml:264-293`에 `pm` 에이전트가 선언되어 있으나 `agents/pm.md` 파일이 부재하여, `--team` 설치 후 PM 서브에이전트 스폰이 실패할 가능성이 있다. 다른 15개 에이전트와 동일 패턴(frontmatter + 핵심 역할 + `## Template` + `## Boundary`)으로 생성한다. Task 1에서 rename한 새 컨벤션(`## Template`)을 직접 사용한다.

**Files:**
- Create: `agents/pm.md`

- [ ] **Step 1: 파일 생성**

다른 에이전트 파일(`agents/architect.md` 등)의 frontmatter 필드를 참조하여 PM 고유 값으로 작성.

파일 내용:

```markdown
---
name: pm
description: PM (Project Manager) - 태스크 분해·DAG 의존성 관리·병렬 스폰·핸드오프·quality gate 감독 오케스트레이터
model: opus
effort: high
color: red
maxTurns: 40
tools:
  - Read
  - Glob
  - Grep
  - Bash
  - Task
  - TodoWrite
  - mcp__serena__find_symbol
  - mcp__serena__list_dir
---

# PM (Project Manager) Agent

프로젝트 오케스트레이션을 미션 크리티컬 조정 업무로서 소유한다 — 단순 태스크 라우팅이 아니다.

## 핵심 역할
- 요청 분석 및 원자적 태스크 분해
- 에이전트 할당 및 스케줄링 (의존성 DAG)
- 병렬 실행 가능 태스크 그룹화·동시 스폰
- 진행 상황 체크포인트 및 블로커 조기 감지
- 각 phase quality gate 통과 여부 검증
- 코드 리뷰 종합 판정 및 tiebreaker
- 최종 보고서 통합 및 컨텍스트 릴레이 감독

## 인지 전략
- Critical path analysis
- Resource leveling
- Risk-first prioritization

## Template
표준 5섹션 템플릿 적용. 상세 프롬프트: `~/.claude/team/prompts/pm.md`

## Boundary
- 코드 직접 구현 금지 — 구현은 Developer 에이전트에 위임
- 아키텍처 결정을 단독으로 내리지 않는다 — 필요 시 Architect 선 스폰
- 부모가 명시적으로 요청하지 않는 한 quality gate 우회·생략 금지
```

**Rationale (frontmatter 값 근거):**
- `model: opus` — `agents.yaml:14` `pm` 라우팅 기본값 및 Decision Log 3의 PM Tiebreaker=opus 규약
- `effort: high` — 분해·조율이 중복 추론 요구 (architect와 동일 effort)
- `color: red` — 다른 15개 색(blue/orange/green/purple 등)과 구별; 오케스트레이터 단독 색
- `maxTurns: 40` — 최대값 (architect 25보다 상향, 서브에이전트 스폰 왕복 커버)
- `tools` — Bash + Task(서브에이전트 스폰 필수) + TodoWrite + Serena 파일 계층 탐색
- **isolation 필드 제외** — PM은 메인 오케스트레이터, worktree 격리는 Developer/QA에만 적용

- [ ] **Step 2: 검증 — 패턴 일치 확인**

```bash
cd /Users/jeongsik/develop/claude-code-guide
# 새 파일이 다른 15개와 동일 헤더 컨벤션 사용
grep -c "^## Template$" agents/pm.md
# 기대: 1
grep -c "^## Boundary$" agents/pm.md
# 기대: 1
# 총 16개 agents/*.md 파일 확인
ls agents/*.md | wc -l
# 기대: 16
```

- [ ] **Step 3: agents.yaml과의 이름 매칭 검증**

```bash
AGENT_NAMES=$(awk '/^agents:$/{in_block=1; next} in_block && /^[a-z_]+:/{in_block=0} in_block && /^  [a-z-]+:$/{gsub(/^  |:$/,""); print}' /Users/jeongsik/develop/claude-code-guide/agents.yaml)
for name in $AGENT_NAMES; do
  test -f "/Users/jeongsik/develop/claude-code-guide/agents/${name}.md" || echo "MISSING: $name"
done
# 기대: 출력 없음 (16/16 매칭)
```

---

## Task 3: `prompts/qa.md` → `prompts/qa-engineer.md` rename + 참조 업데이트 (P0-5, B1)

**Why:** strategy §2 B1. 다른 15개 에이전트는 `agents/X.md ↔ prompts/X.md` 파일명이 정확히 일치하는데 `qa`만 단축명을 사용하여 규약이 무너져 있다. `agents.yaml` SSOT의 `qa-engineer` 이름으로 통일한다. git 히스토리 보존을 위해 `git mv` 사용.

**Files:**
- Rename: `prompts/qa.md` → `prompts/qa-engineer.md`
- Modify: `agents/qa-engineer.md` (line 33 경로 참조)
- Modify: `docs/v3-changelog.md` (line 37, 128 historical 참조 — v4.0 이후 표준명 기준으로 갱신)

- [ ] **Step 1: git mv로 파일 rename**

```bash
cd /Users/jeongsik/develop/claude-code-guide
git mv prompts/qa.md prompts/qa-engineer.md
```

- [ ] **Step 2: 검증 — rename 반영 + 파일 수 변화 없음**

```bash
ls prompts/qa.md 2>&1
# 기대: No such file
ls prompts/qa-engineer.md
# 기대: prompts/qa-engineer.md
ls prompts/*.md | wc -l
# 기대: 16 (변화 없음)
git status --short prompts/
# 기대: 'R  prompts/qa.md -> prompts/qa-engineer.md' 1줄
```

- [ ] **Step 3: `agents/qa-engineer.md:33` 경로 참조 수정**

Edit tool로 `agents/qa-engineer.md` 내부 `~/.claude/team/prompts/qa.md` → `~/.claude/team/prompts/qa-engineer.md` 교체.

정확한 old_string:
```
표준 5섹션 템플릿 적용. 상세 프롬프트: `~/.claude/team/prompts/qa.md`
```
정확한 new_string:
```
표준 5섹션 템플릿 적용. 상세 프롬프트: `~/.claude/team/prompts/qa-engineer.md`
```

- [ ] **Step 4: `docs/v3-changelog.md` 2곳 historical 참조 업데이트**

changelog는 "Agents Standardized" 표준화 결과를 기술하는 문서이므로, v4.0에서 표준화가 완성된 이름(qa-engineer)을 반영하는 것이 문서 의도와 부합. v3.0 시점 파일명 그대로 보존하면 현 독자에게 `prompts/qa.md`라는 부재 파일을 가리키는 broken reference가 된다.

Edit 1 — line 37:
- old_string: `` - `qa.md` — QA Engineer ``
- new_string: `` - `qa-engineer.md` — QA Engineer ``

Edit 2 — line 128:
- old_string: `` ### QA Engineer (`qa.md`) — 330 lines ``
- new_string: `` ### QA Engineer (`qa-engineer.md`) — 330 lines ``

- [ ] **Step 5: 최종 전수 검증 — 잔여 `prompts/qa.md` 또는 단독 `qa.md` 참조 0건**

```bash
cd /Users/jeongsik/develop/claude-code-guide
grep -rn "\bqa\.md\b\|prompts/qa\b" . \
  --include="*.md" --include="*.sh" --include="*.yaml" --include="*.yml" 2>/dev/null \
  | grep -v "\.audit/\|docs/v4/\|\.git/"
# 기대: 출력 없음 (strategy.md와 이 plan.md는 docs/v4/이므로 제외됨)
```

---

## 🔔 Checkpoint (a): validate-system.sh 수정 직전

**이 시점 repo 상태:**
- `agents/*.md` 16개 파일 모두 `## Template` 헤더 (Task 1+2 결과)
- `agents/pm.md` 신규 존재, agents.yaml과 파일 시스템 매칭 복원
- `prompts/qa-engineer.md` 존재, `prompts/qa.md` 부재
- 모든 repo 내부 참조가 `qa-engineer.md` 가리킴
- **`scripts/validate-system.sh`는 아직 수정 전** — 현재 실행 시 line 43의 `## v3.0 Template` 기대가 15개 agent에서 FAIL, line 217의 `qa`가 `prompts/qa.md`(부재) 참조로 WARNING 다수

**재개 전 확인:**
- `git status`로 의도한 변경만 staged/unstaged 상태인가
- 다음 Task 4에서 `validate-system.sh` 수정 후 0 errors/0 warnings로 회복되는 것이 목표

→ 사용자 확인 후 Task 4 진행.

---

## Task 4: `scripts/validate-system.sh` 리팩터링 (P0-2 / P0-4, B3)

**Why:** 단일 스크립트 안에서 (a) Task 1의 헤더 rename 반영, (b) agents.yaml을 SSOT로 삼는 동적 파싱으로 전환, (c) P0-1이 추가한 pm.md 누락을 validate가 감지하도록 신규 체크 추가. 네 항목이 모두 같은 파일을 수정하므로 한 번에 처리.

**Files:**
- Modify: `scripts/validate-system.sh`

- [ ] **Step 1: 상단 초기화 블록에 동적 변수 추가 (라인 19 근처 — `echo "=== ..."` 바로 직전)**

해당 변수들은 모든 다운스트림 체크에서 사용되므로 초기 변수 선언부(line 7-18) 확장 구역에 배치.

추가할 블록 (line 18과 line 20 사이 삽입, 원본 `echo "=== Multi-Agent Team System v3.2 Validation ==="` 직전):

```bash
# v4.0 P0-4: agents.yaml을 SSOT로 삼는 동적 에이전트 이름 파싱
# bash grep 기반 — yq 외부 의존 회피 (strategy.md Decision 3)
if [ -f "$AGENTS_YAML" ]; then
    ALL_AGENTS=$(awk '/^agents:$/{in_block=1; next} in_block && /^[a-z_]+:/{in_block=0} in_block && /^  [a-z-]+:$/{gsub(/^  |:$/,""); print}' "$AGENTS_YAML")
    REVIEWERS=$(echo "$ALL_AGENTS" | grep -E -- '-reviewer$' || true)
    # domain_specific = 리뷰어·엔지니어·PM을 제외한 에이전트 (terminology 체크 skip 대상)
    DOMAIN_SPECIFIC=$(echo "$ALL_AGENTS" | grep -vE -- '-reviewer$|^(developer|qa-engineer|architect|pm)$' || true)
else
    ALL_AGENTS=""
    REVIEWERS=""
    DOMAIN_SPECIFIC=""
fi
```

**주의:** `if [ -f "$AGENTS_YAML" ]` 보호는 `~/.claude/team/agents.yaml`이 미설치인 경우(설치 전 실행)에 스크립트가 set -e에서 죽지 않도록 하기 위함. 이 경우 기존 체크 블록 3 (line 98-104)이 파일 부재를 FAIL로 감지.

- [ ] **Step 2: Line 38-53 블록 (체크 2: Subagent Definition Files) 수정**

old 블록 (line 43):
```bash
    if ! grep -q "## v3.0 Template" "$agent" 2>/dev/null; then
        echo "ERROR: $filename missing '## v3.0 Template'"
```

new:
```bash
    if ! grep -q "^## Template$" "$agent" 2>/dev/null; then
        echo "ERROR: $filename missing '## Template'"
```

**주의:** 정규식 앵커 추가(`^...$`)로 `## Template (legacy)` 같은 부분 매칭을 차단. Task 1의 sed rename도 동일 앵커를 사용했으므로 정확 매칭 유지.

- [ ] **Step 3: 체크 2 확장 — agents.yaml ↔ agents/*.md 파일 매칭 검증 추가 (P0-1 게이트)**

line 52 (`AGENT_COUNT=$(ls -1 ... )`) 직전에 삽입:

```bash
# v4.0 P0-1: agents.yaml 선언 ↔ agents/*.md 파일 매칭
if [ -n "$ALL_AGENTS" ]; then
    for agent_name in $ALL_AGENTS; do
        if [ ! -f "$AGENTS_DIR/$agent_name.md" ]; then
            echo "ERROR: agents.yaml declares '$agent_name' but $agent_name.md not found"
            ERRORS=$((ERRORS + 1))
        fi
    done
fi
```

**이 체크가 감지하는 drift:** 이번 P0-1로 추가한 pm.md가 향후 누락되거나, 새 에이전트가 yaml에 선언되었지만 md 파일이 없을 때 즉시 FAIL.

- [ ] **Step 4: Line 217 REVIEWER_PROMPTS 동적화 (체크 8: Confidence Scoring)**

old:
```bash
REVIEWER_PROMPTS=("security-reviewer" "performance-reviewer" "test-coverage-reviewer" "accessibility-reviewer" "ux-reviewer" "api-reviewer" "code-reviewer" "qa")
```

new:
```bash
# v4.0 P0-4: REVIEWERS($AGENTS_YAML에서 동적 추출) + qa-engineer(P0-5 rename 후 정식명)
REVIEWER_PROMPTS=()
while IFS= read -r r; do
    [ -n "$r" ] && REVIEWER_PROMPTS+=("$r")
done <<< "$REVIEWERS"
REVIEWER_PROMPTS+=("qa-engineer")
```

그리고 그 아래의 `echo "  Confidence scoring: $CONF_COUNT/8 reviewers"`(line 227)를 동적 개수로 변경:

old:
```bash
echo "  Confidence scoring: $CONF_COUNT/8 reviewers"
```

new:
```bash
echo "  Confidence scoring: $CONF_COUNT/${#REVIEWER_PROMPTS[@]} reviewers"
```

- [ ] **Step 5: Line 237 terminology 체크 skip 리스트 동적화 (체크 9)**

**설계 의도:** 원 코드의 skip 리스트 = `pm dba publisher documenter explorer designer` = 리뷰어·일반 엔지니어가 아닌 domain-specific 에이전트. Step 1의 `DOMAIN_SPECIFIC` 정의(`ALL_AGENTS`에서 `-reviewer$`와 `developer|qa-engineer|architect|pm` 제외)는 `dba, designer, publisher, documenter, explorer`의 5개를 포함하고 pm은 포함하지 않음(pm은 orchestrator로서 별도 skip 필요). 따라서 skip 조건 = `filename in DOMAIN_SPECIFIC ∪ {pm.md}`.

old:
```bash
    if [ "$filename" = "pm.md" ] || [ "$filename" = "dba.md" ] || [ "$filename" = "publisher.md" ] || [ "$filename" = "documenter.md" ] || [ "$filename" = "explorer.md" ] || [ "$filename" = "designer.md" ]; then
        continue
    fi
```

new:
```bash
    # v4.0 P0-4: DOMAIN_SPECIFIC(agents.yaml 동적 추출) + pm(orchestrator, 별도 skip)
    skip=0
    case "$filename" in
        pm.md) skip=1 ;;
    esac
    if [ "$skip" = "0" ]; then
        for ds in $DOMAIN_SPECIFIC; do
            if [ "$filename" = "${ds}.md" ]; then
                skip=1
                break
            fi
        done
    fi
    if [ "$skip" = "1" ]; then
        continue
    fi
```

- [ ] **Step 6: 하드코딩 유지 구간에 설계 의도 주석 추가 (Lines 74, 82, 90)**

각 블록 직전에 한 줄 주석:

Line 73 직전:
```bash
# 선택 검증: isolation은 파일 수정권을 가진 developer·qa-engineer 에이전트에만 요구되는 설계 정책 (drift 재생산 엔진 아님, 하드코딩 유지)
```

Line 81 직전:
```bash
# 선택 검증: memory: project는 장기 컨텍스트 축적이 필요한 핵심 리뷰어 4종에만 요구되는 설계 정책 (하드코딩 유지)
```

Line 89 직전:
```bash
# 선택 검증: disallowedTools는 탐색 전용 explorer에만 요구되는 설계 정책 (하드코딩 유지)
```

- [ ] **Step 7: 로컬 실행 검증 — validate-system.sh 의존성 체크**

validate-system.sh는 `$HOME/.claude/team/` 경로를 참조한다. 이 경로는 `install-skills.sh --team` 실행 후 생성됨. 로컬 검증 시에는 두 가지 경로가 있다:

경로 A(권장): `--team` 재설치 후 실행
```bash
cd /Users/jeongsik/develop/claude-code-guide
bash scripts/install-skills.sh --team --force
bash scripts/validate-system.sh
echo "exit=$?"
```
기대: `Errors: 0`, `Warnings: 0` 또는 `... Warnings: N` (v3.1 frontmatter 경고 등은 이번 스코프 밖이므로 기존 경고 수 유지 허용). exit 0.

경로 B: 설치 없이 `$HOME/.claude/team/` 임시 생성 후 스크립트 실행 — 이 plan의 스코프 밖. 경로 A 우선.

**허용되는 경고 기준선:**
- Step 1 초기화 블록이 `$AGENTS_YAML` 부재 시 빈 변수로 fallback되어 P0-1 신규 체크가 skip됨 — 이는 설치 전 단독 실행 UX 보존 목적. 설치 후 실행에서는 정상 동작.

- [ ] **Step 8: 리그레션 검증 — pm.md 누락 시 새 체크가 ERROR를 감지하는지 확인**

validate-system.sh는 `$HOME/.claude/agents/`(설치 타겟)를 스캔하므로, 시뮬레이션은 **설치 타겟을 직접 임시 이동**하는 것이 정확 (repo source를 옮기면 installer가 source 부재를 감지해 copy를 skip하거나 기존 타겟을 그대로 두는 행동이 설치 스크립트 구현에 의존적이므로 신뢰할 수 없음).

```bash
# 설치 타겟의 pm.md를 잠시 이동
mv "$HOME/.claude/agents/pm.md" /tmp/pm.md.test
bash /Users/jeongsik/develop/claude-code-guide/scripts/validate-system.sh 2>&1 | grep "pm"
# 기대: "ERROR: agents.yaml declares 'pm' but pm.md not found" 한 줄

# 복원
mv /tmp/pm.md.test "$HOME/.claude/agents/pm.md"
bash /Users/jeongsik/develop/claude-code-guide/scripts/validate-system.sh 2>&1 | grep -c "^ERROR"
# 기대: 0
```

**주의:** 설치 타겟(`~/.claude/agents/`)에 pm.md가 존재해야 이 시뮬레이션이 유효하므로 **Step 7 실행(즉 `install-skills.sh --team --force`) 후** 진행. 시뮬레이션 완료 후 반드시 타겟 파일을 원복.

---

## 🔔 Checkpoint (b): agents/*.md 헤더 rename 직전 — **이미 Task 1에서 완료됨**

**이 체크포인트는 원 제안의 순서 기반이며, 실제 내부 작업 순서는 (Task 1 헤더 rename → Task 2 pm.md 생성 → Task 3 qa rename → Task 4 validate 수정)으로 재배열됨.** 재배열 근거:

- pm.md를 `## Template`로 만들려면 검증 스크립트가 `## Template`를 기대해야 하고, 검증 스크립트 변경 전에 기존 15개 파일이 먼저 `## Template`로 통일되어야 중간 상태 부정합 방지.
- qa rename은 validate-system.sh 동적 파싱 변경과 독립적 파일 집합(prompts/, agents/qa-engineer.md, docs/v3-changelog.md)이라 Task 3에서 분리.
- validate-system.sh는 모든 파일 변경의 **소비자(consumer)** 로서 마지막에 일괄 수정.

**따라서 Checkpoint (b)는 Checkpoint (a)와 사실상 동일 시점** — 사용자 확인은 CP (a) 한 번으로 통합.

---

## Task 5: 전체 검증 + 커밋 전 최종 점검

- [ ] **Step 1: `git status` 및 `git diff --stat` 검토**

```bash
cd /Users/jeongsik/develop/claude-code-guide
git status
git diff --stat
```

기대 변경 요약:
- `A  agents/pm.md` — 신규
- `M  agents/*.md` (15개) — 헤더 rename 1줄 변경씩
- `M  agents/qa-engineer.md` — 추가 1줄 변경(prompt 경로)
- `R  prompts/qa.md -> prompts/qa-engineer.md` — rename
- `M  docs/v3-changelog.md` — 2줄 변경
- `M  scripts/validate-system.sh` — 5개 블록 수정
- `A  docs/v4/plans/2026-04-22-p0-bundle-a.md` — 이 plan 문서

- [ ] **Step 2: validate-system.sh 최종 실행 — 0 errors 확인**

```bash
bash scripts/install-skills.sh --team --force
bash scripts/validate-system.sh
echo "exit_code=$?"
```

기대: Errors: 0, exit_code=0. 경고 수는 변경 전 기준선(pre-P0 기준선)과 동일하거나 감소.

- [ ] **Step 3: 참조 무결성 재검증**

```bash
# (1) prompts/qa.md 흔적 0
grep -rn "\bqa\.md\b\|prompts/qa\b" . \
  --include="*.md" --include="*.sh" --include="*.yaml" --include="*.yml" 2>/dev/null \
  | grep -v "\.audit/\|docs/v4/\|\.git/"
# 기대: 출력 없음

# (2) ## v3.0 Template 흔적 0 (.audit/, docs/v4/ 제외)
grep -rn "## v3.0 Template" . \
  --include="*.md" --include="*.sh" 2>/dev/null \
  | grep -v "\.audit/\|docs/v4/"
# 기대: 출력 없음

# (3) agents.yaml의 16개 에이전트 ↔ agents/*.md 16개 1:1 매칭
AGENT_NAMES=$(awk '/^agents:$/{in_block=1; next} in_block && /^[a-z_]+:/{in_block=0} in_block && /^  [a-z-]+:$/{gsub(/^  |:$/,""); print}' agents.yaml)
echo "$AGENT_NAMES" | wc -l
# 기대: 16
for name in $AGENT_NAMES; do
  test -f "agents/${name}.md" || echo "MISSING: $name"
done
# 기대: 출력 없음
```

---

## 🔔 Checkpoint (c): 커밋 직전

**이 시점 repo 상태:**
- Task 5 Step 2/3 모두 PASS
- 기대 변경 요약과 `git diff --stat` 일치

**사용자 확인 후 Task 6 진행.**

---

## Task 6: 단일 커밋 생성

- [ ] **Step 1: 스테이징**

```bash
cd /Users/jeongsik/develop/claude-code-guide
git add agents/ prompts/ docs/v3-changelog.md scripts/validate-system.sh docs/v4/plans/2026-04-22-p0-bundle-a.md
git status --short
```

`git mv prompts/qa.md prompts/qa-engineer.md`는 Task 3 Step 1에서 이미 rename으로 staged 상태이므로 `git add prompts/`는 추가 변경(있을 시)만 병합. 기대 `git status --short` 출력:
- `A  agents/pm.md`
- `M  agents/{accessibility,api,architect,code-reviewer,dba,designer,developer,documenter,explorer,performance-reviewer,publisher,qa-engineer,security-reviewer,test-coverage-reviewer,ux-reviewer}.md` (15개, qa-engineer는 header + 경로 2줄 변경)
- `R  prompts/qa.md -> prompts/qa-engineer.md`
- `M  docs/v3-changelog.md`
- `M  scripts/validate-system.sh`
- `A  docs/v4/plans/2026-04-22-p0-bundle-a.md`

- [ ] **Step 2: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(v4 P0-A): P0 Bundle A — pm.md 생성 + qa rename + validate 동적 파싱

strategy.md §3 P0 백로그 중 4건을 단일 커밋으로 구현. 네 항목이 모두
scripts/validate-system.sh를 공통 수정점으로 교차하므로 분리 불가.

변경 사항:
- P0-1: agents/pm.md 신규 생성 (다른 15개와 동일 패턴 + `## Template`
  헤더 신 컨벤션 바로 적용). agents.yaml pm 선언과 파일 시스템 매칭 복원.
- P0-2: agents/*.md 15개의 `## v3.0 Template` → `## Template` 일괄 rename
  + validate-system.sh:43 기대값 동반 변경. 자기 모순(스크립트는 v3.2
  canonical을 검증하면서 agent 파일엔 v3.0 헤더 강제) 제거.
- P0-4: validate-system.sh 에이전트 이름 하드코딩 → agents.yaml 동적
  파싱(awk/grep, yq 미도입 — strategy Decision 3 준수). line 217
  REVIEWER_PROMPTS·line 237 skip 리스트 동적화. 신규 체크 추가:
  agents.yaml ↔ agents/*.md 파일 매칭(pm.md 누락류 drift 자동 감지).
  lines 74/82/90 하드코딩은 선택 검증 설계 의도이므로 주석으로 명시 후 유지.
- P0-5 (B1): prompts/qa.md → prompts/qa-engineer.md rename +
  agents/qa-engineer.md:33 경로 참조 + docs/v3-changelog.md 2건 갱신.
  단축명 1예외가 다른 15쌍 SSOT 규약을 무너뜨리던 문제 종료.

Breaking Changes (strategy.md §2 Migration Note 반영):
- B1: prompts/qa.md 파일명 변경. Scenario A(미수정 사용자)는
  `bash scripts/install-skills.sh --force` 재설치로 자동 반영. Scenario B
  (CUSTOMIZE 수정 사용자)는 `prompts/qa-engineer.md` 신규 파일에 수정
  사항 수동 이식 필요.
- B3: validate-system.sh가 agents.yaml 파싱에 의존. yq 대신 bash
  awk/grep 사용으로 외부 의존성 0개. Scenario A/B 모두 재설치로 자동 반영.

검증:
- `bash scripts/install-skills.sh --team --force && bash scripts/validate-system.sh`
  → Errors: 0, exit 0
- agents.yaml 16개 ↔ agents/*.md 16개 1:1 매칭
- 잔여 `prompts/qa.md` / `## v3.0 Template` 참조 0건

남은 P0: 없음 (P0-3은 커밋 31b0a17에서 완료, Bundle A로 P0 전체 마감).
다음 슬라이스: P1 착수 가능 (우선순위 P1-1 버전 canonical 통일).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 3: 커밋 사후 검증**

```bash
git log --oneline -1
git show --stat HEAD
git status
```

기대: 최신 커밋 = feat(v4 P0-A), working tree clean.

---

## Remaining Out-of-Scope Items

이 plan 실행 후에도 남는 v4.0 백로그 (strategy.md §3 기준):

| # | 항목 | Bundle |
|---|------|--------|
| P1-1 | 버전 숫자 canonical 통일 | B |
| P1-2 | 프리셋 정의 6-스킬 중복 제거 | C |
| P1-3 | 에이전트 목록 canonical 통일 | A (일부 선행) |
| P1-4 | 스킬 목록 canonical 통일 | B |
| P1-5 | `hooks/scripts/*.sh` rename (B2) | D |
| P1-6 | Complexity Tier 임계값 통일 | C |
| P1-7~9 | description 재작성 3건 | C |
| P2-1~10 | 문서 drift 정리 | E |

다음 실행 세션에서 P1 bundle을 확정.
